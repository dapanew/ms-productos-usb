services:
  factura-1:
    image: factura:1
    container_name: factura-1
    environment:
      - DATABASE_URL=jdbc:postgresql://localhost:5432/demo
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=admind
      - SERVER_PORT=8080
    ports:
      - "9090:8080"
    networks:
      - curso
    restart: always

